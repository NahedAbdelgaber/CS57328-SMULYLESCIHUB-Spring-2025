package com.cshub.backend.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.cshub.backend.model.RAApplication;

public interface RAApplicationRepository extends JpaRepository<RAApplication, Long> {
}
